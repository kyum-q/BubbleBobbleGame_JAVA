package BubbleGame;

import javax.swing.JFrame;

import BubbleGame.gameObject.bubble.Bubble;
import utility.Settings;

public class Main {
	public static void main(String[] args) {
		BubbleBobbleGame game = new BubbleBobbleGame();
		/*
		EndingPanel ending = new EndingPanel("GAME CLEAR !!!","HAPPY");
		JFrame f = new JFrame();
		f.setSize((int)Settings.SCENE_HEIGHT,(int) Settings.SCENE_WIDTH);
		f.setVisible(true);
		f.setContentPane(ending);
		*/
	}
}
